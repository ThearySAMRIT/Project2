<div class="tweetEntry-tweetHolder">
  <h3 class="tweetEntry">
    <%= link_to gravatar_for(post.user), post.user %>
    <span class="title"><%= post.title %></span>
  </h3>
  <div class="tweetEntry">
    <div class="tweetEntry-content">
      <strong class="tweetEntry-fullname">
        <span class="user"><%= link_to post.user.full_name, post.user %></span>
      </strong>
      <span class="timestamp"><%= t ".posted" %> <%= time_ago_in_words post.created_at %> ago.
      </span>
      <span class="timestamp"><%= t ".edited" %><%= time_ago_in_words post.updated_at %> ago.
      </span>
      <div class="tweetEntry-text-container">
        <span class="content"><%= post.content %></span>
      </div>
    </div>
    <div class="optionalMedia"><%= image_tag post.picture.url if post.picture? %></div>
    <div class="footer">
      <% if current_user? post.user %>
        <%= link_to "edit", edit_post_path(post), remote: :true, data: {toggle: "modal", target: "#myModal"}%>
        <%= link_to "comment", post %>
        <%= link_to "delete", post, method: :delete, data: {confirm: "You sure?"} %>
      <% else %>
      <div class="tweetEntry-content">
        <%= link_to "Comment", post %>
      </div>
      <% end %>
    </div>
  </div>
</div>
