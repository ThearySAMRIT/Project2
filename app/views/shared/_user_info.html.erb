<%= link_to gravatar_for(current_user, size: Settings.profile.size), current_user %>
<h2><%= current_user.full_name %></h2>
<span><%= link_to t(".view"), current_user %></span>
<span><%= pluralize current_user.posts.count, t(".posts") %></span>
